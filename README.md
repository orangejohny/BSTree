# Project BSTree

## UNIX-systems

**Building of a project:**

```ShellSession
$ cmake -H./sources -Bbuild
$ cmake --build build
```

**Using of program:**

Now program is ready for use in build directory

```ShellSession
$ cd build
$ ./BSTree <item1> <item2> ...
```